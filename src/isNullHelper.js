/**
 * Checks if an object is null.
 *
 * @method isNull
 * @param {any} object Object we want to know the type.
 * @return {Boolean} Returns true if the object is null and false otherwise.
 */
module.exports = (object) => null === object;
