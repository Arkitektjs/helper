/**
 * Checks if an object is true.
 *
 * @method isTrue
 * @param {any} object Object we want to know the type.
 * @return {Boolean} Returns true if the object is true and false otherwise.
 */
module.exports = (object) => true === object;
