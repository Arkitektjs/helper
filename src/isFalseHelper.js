/**
 * Checks if an object is false.
 *
 * @method isFalse
 * @param {any} object Object we want to know the type.
 * @return {Boolean} Returns false if the object is false and false otherwise.
 */
module.exports = (object) => false === object;
