/**
 * Checks if an object is undefined.
 *
 * @method isUndefined
 * @param {any} object Object we want to know the type.
 * @return {Boolean} Returns true if the object is undefined and false otherwise.
 */
module.exports = (object) => undefined === object;
