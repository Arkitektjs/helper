/**
 * Generates random integer.
 *
 * @method randomInt
 * @param {Number} max Value will be between 0 and max.
 * @return {Number} Returns random integer.
 */
module.exports = (max) => Math.floor(Math.random() * Math.floor(max));
